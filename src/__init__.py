"""Application package following the agent-service-toolkit layout.

The existing ``app`` package remains the compatibility domain implementation while
the ``src`` package provides the platform-facing registry and service boundary.
"""
